<%- include('../partials/header') %>

<div class="container mt-5">
  <h2 class="text-white">🎬 Danh sách Suất Chiếu</h2>
  <table class="table table-dark table-bordered mt-4">
    <thead>
      <tr>
        <th>Ngày giờ bắt đầu</th>
        <th>Ngày giờ kết thúc</th>
        <th>Thời gian (phút)</th> <!-- Cột thời gian -->
      </tr>
    </thead>
    <tbody>
      <% danhSachSuatChieu.forEach(show => { %>
        <tr>
          <!-- Hiển thị ngày giờ bắt đầu -->
          <td><%= new Date(show.ngayGioBatDau).toLocaleString('vi-VN') %></td>
          <!-- Hiển thị ngày giờ kết thúc -->
          <td><%= new Date(show.ngayGioKetThuc).toLocaleString('vi-VN') %></td>
          <!-- Tính thời gian chiếu -->
          <td><%= Math.floor((new Date(show.ngayGioKetThuc) - new Date(show.ngayGioBatDau)) / 60000) %> phút</td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<%- include('../partials/footer') %>
