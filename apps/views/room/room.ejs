<%- include('../partials/header') %>

<div class="container mt-5">
  <h2 class="text-white">🛋️ Danh sách Phòng Chiếu (Room)</h2>
  <table class="table table-dark table-bordered mt-4">
    <thead>
      <tr>
        <th>Tên Phòng</th>
        <th>Sức Chứa</th>
      </tr>
    </thead>
    <tbody>
      <% danhSachRoom.forEach(room => { %>
        <tr>
          <td><%= room.tenPhong %></td>
          <td><%= room.sucChua %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<%- include('../partials/footer') %>
