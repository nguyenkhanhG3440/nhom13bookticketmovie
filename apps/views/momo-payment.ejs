<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thanh toán MoMo</title>
    <style>
        body {
            text-align: center;
            font-family: Arial, sans-serif;
            background-color: #141414;
            color: white;
            padding: 20px;
        }
        .qr-container {
            margin-top: 20px;
        }
        .qr-code {
            width: 250px;
            height: 250px;
        }
        .btn {
            background-color: #e50914;
            padding: 10px 20px;
            color: white;
            font-size: 18px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .btn:hover {
            background-color: #c10812;
        }
    </style>
</head>
<body>
    <h2>Quét mã QR để thanh toán bằng MoMo</h2>
    <div class="qr-container">
        <% if (qrCodeUrl) { %>
            <img src="<%= qrCodeUrl %>" class="qr-code" alt="Mã QR MoMo">
            <p>Số tiền: <%= totalPrice.toLocaleString() %> VND</p>
        <% } else { %>
            <p style="color: red;">Không thể tạo mã QR MoMo!</p>
        <% } %>
    </div>
    <button class="btn" onclick="window.location.href='/home'">Quay lại</button>
</body>
</html>
